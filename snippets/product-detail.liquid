{% comment %}
  Product Detail Snippet
  Shared code for PDP and Quickview with Customizer variables support.
{% endcomment %}

{%- liquid
  assign current_variant = product.selected_or_first_available_variant
  assign images = product.images
  assign featured_image = current_variant.featured_image | default: product.featured_image

  assign is_out_of_stock = true
  if product.available
    assign is_out_of_stock = false
  endif

  assign is_on_sale = false
  assign compare_at_price = current_variant.compare_at_price
  assign price = current_variant.price
  if compare_at_price > price
    assign is_on_sale = true
  endif

  assign product_title_lower = product.title | downcase
  assign product_type_lower = product.type | downcase
  assign product_tags_string = product.tags | join: ' ' | downcase
  assign combined_text = product_title_lower | append: ' ' | append: product_type_lower | append: ' ' | append: product_tags_string | append: ' ' | append: product.handle
  
  if request.locale.iso_code == 'ar'
    assign size_chart_title = 'جدول المقاسات'
  else
    assign size_chart_title = 'Size Chart'
  endif
  assign size_chart_key = 'default'

  if combined_text contains 'yellow' or combined_text contains 'pink top'
    assign size_chart_key = 'none'
  elsif combined_text contains 'a new era' and combined_text contains 'crop'
    assign size_chart_key = 'a-new-era-cropped'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات سويت شيرت كروب A New Era'
    else
      assign size_chart_title = 'A New Era Cropped Sweatshirt Size Chart'
    endif
  elsif combined_text contains 'turtleneck'
    assign size_chart_key = 'turtleneck'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات سويت شيرت هاي كول أوفر سايز'
    else
      assign size_chart_title = 'Oversized Sweatshirt Turtleneck Size Chart'
    endif
  elsif combined_text contains 'double collar' and combined_text contains 'crop'
    assign size_chart_key = 'women-cropped'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات سويت شيرت دبل كولار نسائي'
    else
      assign size_chart_title = 'Double Collar Cropped Sweatshirt Size Chart'
    endif
  elsif combined_text contains 'hoodie'
    assign size_chart_key = 'hoodie'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات الهوديز للجنسين'
    else
      assign size_chart_title = 'Unisex Hoodies Size Chart'
    endif
  elsif combined_text contains 'staple' or combined_text contains 'sand'
    assign size_chart_key = 'staple'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات Staple Sand للجنسين'
    else
      assign size_chart_title = 'Staple Sand Unisex Size Chart'
    endif
  elsif combined_text contains 'linen'
    assign size_chart_key = 'linen'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات طقم الكتان'
    else
      assign size_chart_title = 'Linen Shirt & Shorts Size Chart'
    endif
  elsif combined_text contains 't-shirt' or combined_text contains 'tshirt' or combined_text contains 't shirt' or combined_text contains 'tee' or combined_text contains 'top'
    assign size_chart_key = 'tshirt'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات التيشيرتات للجنسين'
    else
      assign size_chart_title = 'Unisex T-Shirt Size Chart'
    endif
  elsif combined_text contains 'set'
    assign size_chart_key = 'linen'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات طقم الكتان'
    else
      assign size_chart_title = 'Linen Shirt & Shorts Size Chart'
    endif
  elsif combined_text contains 'crop'
    assign size_chart_key = 'women-cropped'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات سويت شيرت دبل كولار نسائي'
    else
      assign size_chart_title = 'Double Collar Cropped Sweatshirt Size Chart'
    endif
  elsif combined_text contains 'sweatshirt' or combined_text contains 'sweater'
    assign size_chart_key = 'sweatshirt'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات السويت شيرت للجنسين'
    else
      assign size_chart_title = 'Unisex Sweatshirts Size Chart'
    endif
  else
    assign size_chart_key = 'sweatshirt'
    if request.locale.iso_code == 'ar'
      assign size_chart_title = 'جدول مقاسات السويت شيرت للجنسين'
    else
      assign size_chart_title = 'Unisex Sweatshirts Size Chart'
    endif
  endif

  if product.handle == 'apricity-blush' or product.handle == 'apy-butter-set' or combined_text contains 'yellow' or combined_text contains 'pink' or combined_text contains 'blush' or product.handle contains 'yellow' or product.handle contains 'pink' or product.handle contains 'blush'
    assign size_chart_key = 'none'
  endif
-%}

{% capture size_chart_html %}
  {% case size_chart_key %}
    {% when 'turtleneck' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الصدر{% else %}1/2 Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الحاشية{% else %}1/2 Bottom{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>69</td><td>58</td><td>66</td><td>46</td><td>67</td></tr><tr style="text-align:center"><td>M/L</td><td>71</td><td>59</td><td>68</td><td>49</td><td>69</td></tr><tr style="text-align:center"><td>L/XL</td><td>73</td><td>60</td><td>71</td><td>51</td><td>71</td></tr></tbody></table>
    {% when 'women-cropped' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الصدر{% else %}1/2 Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الحاشية{% else %}1/2 Bottom{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>XS/S</td><td>43</td><td>52</td><td>46</td><td>37</td><td>55</td></tr><tr style="text-align:center"><td>S/M</td><td>45</td><td>53</td><td>48.5</td><td>39.5</td><td>57</td></tr><tr style="text-align:center"><td>M/L</td><td>47</td><td>54</td><td>51</td><td>42</td><td>59</td></tr></tbody></table>
    {% when 'a-new-era-cropped' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الصدر{% else %}1/2 Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الحاشية{% else %}1/2 Bottom{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>XS/S</td><td>44</td><td>59</td><td>52</td><td>43</td><td>59</td></tr><tr style="text-align:center"><td>S/M</td><td>46</td><td>60</td><td>53</td><td>44</td><td>60</td></tr><tr style="text-align:center"><td>M/L</td><td>48</td><td>61</td><td>54</td><td>45</td><td>62</td></tr></tbody></table>
    {% when 'hoodie' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الصدر{% else %}1/2 Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الحاشية{% else %}1/2 Bottom{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>69</td><td>54-56</td><td>62-63</td><td>54-58</td><td>65-67</td></tr><tr style="text-align:center"><td>M/L</td><td>71</td><td>57-58</td><td>64-66</td><td>56-60</td><td>68-69</td></tr><tr style="text-align:center"><td>L/XL</td><td>73</td><td>59</td><td>67-69</td><td>59-64</td><td>70-74</td></tr></tbody></table>
    {% when 'tshirt' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الصدر{% else %}Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>70</td><td>23</td><td>55</td><td>48</td></tr><tr style="text-align:center"><td>M/L</td><td>75</td><td>25.5</td><td>60</td><td>50</td></tr><tr style="text-align:center"><td>L/XL</td><td>78</td><td>26.5</td><td>62</td><td>53</td></tr></tbody></table>
    {% when 'linen' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الخصر (مطاط){% else %}Waist (elastic){% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}طول الشورت{% else %}Shorts Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}طول القميص{% else %}Shirt Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الصدر{% else %}Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}طول الكم{% else %}Sleeve Length{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>33</td><td>43</td><td>64</td><td>51</td><td>46</td><td>23</td></tr><tr style="text-align:center"><td>M/L</td><td>37</td><td>48</td><td>67</td><td>54</td><td>48</td><td>27</td></tr><tr style="text-align:center"><td>L/XL</td><td>43</td><td>50</td><td>70</td><td>57</td><td>50</td><td>30</td></tr></tbody></table>
    {% when 'staple' %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الصدر{% else %}Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Sholder{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>68</td><td>26.5</td><td>57</td><td>57</td></tr><tr style="text-align:center"><td>M/L</td><td>74</td><td>28</td><td>62</td><td>59</td></tr><tr style="text-align:center"><td>L/XL</td><td>76</td><td>29</td><td>66.5</td><td>62</td></tr></tbody></table>
    {% when 'none' %}
    {% else %}
      <table width="100%"><tbody><tr><td></td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الطول{% else %}Length{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكم{% else %}Sleeve{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الصدر{% else %}1/2 Chest{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}1/2 الحاشية{% else %}1/2 Bottom{% endif %}</td><td style="text-align:center">{% if request.locale.iso_code == 'ar' %}الكتف{% else %}Shoulder{% endif %}</td></tr><tr style="text-align:center"><td>S/M</td><td>69-70</td><td>54</td><td>66-67</td><td>58-59</td><td>68</td></tr><tr style="text-align:center"><td>M/L</td><td>71-72</td><td>55-56</td><td>68-69</td><td>60-61</td><td>69-70</td></tr><tr style="text-align:center"><td>L/XL</td><td>73</td><td>57</td><td>70</td><td>62</td><td>71</td></tr></tbody></table>
  {% endcase %}
{% endcapture %}
{%- assign size_chart_html = size_chart_html | strip -%}

<div class="product-detail-wrapper flex flex-col lg:flex-row w-full h-full" data-product-handle="{{ product.handle }}" data-lang="{{ request.locale.iso_code }}">
  
  <!-- LEFT COLUMN: Image Gallery -->
  <div class="product-detail-gallery w-full lg:w-1/2 bg-zinc-50 p-6 flex flex-col gap-3 lg:h-full lg:overflow-y-auto" style="-webkit-overflow-scrolling: touch;">
    <div class="w-full bg-white rounded-2xl overflow-hidden relative">
      <img 
        src="{{ featured_image | image_url: width: 1000 }}" 
        alt="{{ product.title | escape }}" 
        class="product-main-image w-full h-auto object-contain transition-transform duration-700 ease-out hover:scale-110 cursor-zoom-in"
        width="1000" height="1000" />
    </div>
    {%- if images.size > 1 -%}
      <div class="flex gap-2 overflow-x-auto pb-1 product-thumbnails">
        {%- for img in images -%}
          <button
            type="button"
            class="thumbnail-btn flex-shrink-0 w-16 h-16 rounded-xl overflow-hidden border-2 transition {% if featured_image == img %}border-black{% else %}border-transparent{% endif %}"
            data-image-url="{{ img | image_url: width: 1000 }}"
          >
            <img src="{{ img | image_url: width: 150 }}" alt="" class="w-full h-full object-cover" width="150" height="150" />
          </button>
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>

  <!-- RIGHT COLUMN: Details & Option Pickers -->
  <div class="product-detail-info w-full lg:w-1/2 flex flex-col lg:h-full lg:overflow-hidden bg-white">
    <!-- Scrollable Panel -->
    <div class="product-detail-scrollable flex-1 lg:overflow-y-auto px-8 py-10 gap-6 flex flex-col" style="-webkit-overflow-scrolling: touch;">
      
      <!-- Title & Price -->
      <div class="flex flex-col gap-2">
        <h2 class="text-2xl font-bold uppercase leading-tight tracking-tight text-[#1a1a1a]">
          {{ product.title }}
        </h2>
        <div class="flex items-baseline gap-3">
          <span class="product-price-display text-2xl font-semibold tracking-tight text-zinc-900">
            {{ price | money_without_trailing_zeros }}
          </span>
          <span class="product-compare-display text-sm text-zinc-400 line-through {% unless is_on_sale %}hidden{% endunless %}">
            {{ compare_at_price | money_without_trailing_zeros }}
          </span>
        </div>
      </div>

      <!-- Option Selectors -->
      {%- unless product.has_only_default_variant -%}
        <div class="product-options-picker space-y-6">
          {%- for option in product.options_with_values -%}
            {%- assign option_name_lower = option.name | downcase -%}
            
            {%- if option_name_lower == 'color' or option_name_lower == 'colour' -%}
              {%- assign color_option_index = forloop.index0 -%}
              <!-- Color Selector -->
              <div class="option-color-container" data-option-index="{{ color_option_index }}">
                <p class="text-[10px] uppercase tracking-[0.2em] text-zinc-400 mb-3 font-medium">
                  {% if request.locale.iso_code == 'ar' %}اللون —{% else %}Colour —{% endif %} <span class="active-color-label text-zinc-600 normal-case tracking-normal">{{ option.selected_value }}</span>
                </p>
                <div class="flex flex-wrap gap-2">
                  {%- for value in option.values -%}
                    <button
                      type="button"
                      class="color-option-btn px-4 py-2 text-xs rounded-full border transition-all duration-200 tracking-wide {% if option.selected_value == value %}bg-zinc-900 text-white border-zinc-900 shadow-md{% else %}bg-white text-zinc-700 border-zinc-200 hover:border-zinc-500{% endif %}"
                      data-value="{{ value }}"
                    >
                      {{ value }}
                    </button>
                  {%- endfor -%}
                </div>
              </div>

            {%- elsif option_name_lower == 'size' or option_name_lower == 'sizes' or option_name_lower contains 'size' -%}
              {%- assign size_option_index = forloop.index0 -%}
              <!-- Size Selector -->
              <div class="option-size-container rounded-2xl border border-zinc-100 bg-zinc-50/80 p-5" data-option-index="{{ size_option_index }}">
                <div class="flex items-center justify-between mb-4">
                  <span class="text-[10px] uppercase tracking-[0.2em] text-zinc-400 font-medium">
                    {% if request.locale.iso_code == 'ar' %}اختر المقاس{% else %}Select Size{% endif %}
                  </span>
                  
                  <span class="stock-status-indicator text-[10px] font-semibold tracking-wide uppercase flex items-center gap-1.5 text-emerald-600">
                    {% if request.locale.iso_code == 'ar' %}متوفر بالمخزون{% else %}In Stock{% endif %}
                  </span>
                </div>

                <div class="grid grid-cols-4 gap-2">
                  {%- for value in option.values -%}
                    {%- liquid
                      assign size_variant = nil
                      for variant in product.variants
                        if variant.options[size_option_index] == value
                          assign size_variant = variant
                        endif
                      endfor
                      assign size_in_stock = false
                      if size_variant != nil and size_variant.available
                        assign size_in_stock = true
                      endif
                      assign qty = size_variant.inventory_quantity
                    -%}
                    <button
                      type="button"
                      {% unless size_in_stock %}disabled{% endunless %}
                      class="size-option-btn relative py-3 text-xs font-semibold rounded-xl border transition-all duration-200 {% if option.selected_value == value %}bg-zinc-900 text-white border-zinc-900 shadow-lg scale-[1.03]{% elsif size_in_stock %}bg-white text-zinc-800 border-zinc-200 hover:border-zinc-400{% else %}bg-white text-zinc-300 border-zinc-100 cursor-not-allowed{% endif %}"
                      data-value="{{ value }}"
                      data-qty="{{ qty }}"
                    >
                      {{ value }}
                    </button>
                  {%- endfor -%}
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
        </div>
      {%- endunless -%}

      <!-- Size Guide Button -->
      {%- if size_chart_html != blank -%}
        <div class="mt-2 flex justify-end">
          <button
            type="button"
            class="size-guide-trigger-btn flex items-center gap-1.5 text-xs font-semibold text-zinc-500 hover:text-zinc-800 transition-colors bg-transparent border-0 outline-none"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-zinc-400">
              <line x1="12" x2="12" y1="5" y2="19"></line>
              <line x1="5" x2="19" y1="12" y2="12"></line>
              <line x1="8" x2="16" y1="16" y2="16"></line>
              <line x1="8" x2="16" y1="8" y2="8"></line>
            </svg>
            <span>{% if request.locale.iso_code == 'ar' %}دليل المقاسات{% else %}Size Guide{% endif %}</span>
          </button>
        </div>
      {%- endif -%}

      <!-- ACCORDION SECTIONS (CONTROLLED BY CUSTOMIZER) -->
      <div class="product-accordions-group mt-4 border-t border-zinc-100">
        
        <!-- Accordion 1 -->
        <div class="border-b border-zinc-100 accordion-item">
          <button type="button" class="accordion-header w-full flex items-center justify-between py-4 text-left group">
            <div class="flex items-center gap-3">
              <img src="{{ 'logo.png' | asset_url }}" alt="" class="h-4 w-4 object-contain opacity-40" width="16" height="16" />
              <span class="text-sm text-zinc-700 font-medium tracking-wide">{{ acc_1_title }}</span>
            </div>
            <span class="accordion-plus text-zinc-400 text-xl transition-transform duration-300">+</span>
          </button>
          <div class="accordion-body hidden pb-5 px-1 text-sm text-zinc-500 leading-relaxed whitespace-pre-line">
            {{ acc_1_text }}
          </div>
        </div>

        <!-- Accordion 2 -->
        <div class="border-b border-zinc-100 accordion-item">
          <button type="button" class="accordion-header w-full flex items-center justify-between py-4 text-left group">
            <div class="flex items-center gap-3">
              <img src="{{ 'logo.png' | asset_url }}" alt="" class="h-4 w-4 object-contain opacity-40" width="16" height="16" />
              <span class="text-sm text-zinc-700 font-medium tracking-wide">{{ acc_2_title }}</span>
            </div>
            <span class="accordion-plus text-zinc-400 text-xl transition-transform duration-300">+</span>
          </button>
          <div class="accordion-body hidden pb-5 px-1 text-sm text-zinc-500 leading-relaxed">
            {%- if acc_2_text != blank -%}
              <div class="whitespace-pre-line">{{ acc_2_text }}</div>
            {%- else -%}
              {{ product.description }}
            {%- endif -%}
          </div>
        </div>

        <!-- Accordion 3 -->
        <div class="border-b border-zinc-100 accordion-item">
          <button type="button" class="accordion-header w-full flex items-center justify-between py-4 text-left group">
            <div class="flex items-center gap-3">
              <img src="{{ 'logo.png' | asset_url }}" alt="" class="h-4 w-4 object-contain opacity-40" width="16" height="16" />
              <span class="text-sm text-zinc-700 font-medium tracking-wide">{{ acc_3_title }}</span>
            </div>
            <span class="accordion-plus text-zinc-400 text-xl transition-transform duration-300">+</span>
          </button>
          <div class="accordion-body hidden pb-5 px-1 text-sm text-zinc-500 leading-relaxed whitespace-pre-line">
            {{ acc_3_text }}
          </div>
        </div>

        <!-- Accordion 4 -->
        <div class="border-b border-zinc-100 accordion-item">
          <button type="button" class="accordion-header w-full flex items-center justify-between py-4 text-left group">
            <div class="flex items-center gap-3">
              <img src="{{ 'logo.png' | asset_url }}" alt="" class="h-4 w-4 object-contain opacity-40" width="16" height="16" />
              <span class="text-sm text-zinc-700 font-medium tracking-wide">{{ acc_4_title }}</span>
            </div>
            <span class="accordion-plus text-zinc-400 text-xl transition-transform duration-300">+</span>
          </button>
          <div class="accordion-body hidden pb-5 px-1 text-sm text-zinc-500 leading-relaxed whitespace-pre-line">
            {{ acc_4_text }}
          </div>
        </div>

      </div>

    </div>

    <!-- STICKY CTA BUTTONS FOOTER -->
    <div class="product-detail-cta-footer flex-shrink-0 border-t border-zinc-100 bg-white px-6 py-4 flex flex-col gap-3">
      <div class="flex items-center justify-center text-[10px] text-zinc-400 tracking-wide uppercase">
        <span>{% if request.locale.iso_code == 'ar' %}دفع إلكتروني آمن{% else %}Secure checkout{% endif %}</span>
      </div>

      <!-- Quantity Selector -->
      <div class="product-qty-row flex items-center justify-between border border-zinc-200 bg-white rounded-xl h-12 w-full px-4">
        <span class="text-xs font-semibold text-zinc-400 uppercase tracking-wider">{% if request.locale.iso_code == 'ar' %}الكمية{% else %}Quantity{% endif %}</span>
        <div class="flex items-center gap-4">
          <button
            type="button"
            class="product-qty-btn-minus w-8 h-8 flex items-center justify-center text-zinc-400 hover:text-zinc-950 disabled:opacity-30 transition-colors rounded-full hover:bg-zinc-50"
            aria-label="Decrease quantity"
            disabled
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="5" x2="19" y1="12" y2="12"></line>
            </svg>
          </button>
          
          <span class="product-qty-display-value font-mono-street text-sm text-zinc-950 font-bold min-w-[1.5rem] text-center">1</span>
          
          <button
            type="button"
            class="product-qty-btn-plus w-8 h-8 flex items-center justify-center text-zinc-400 hover:text-zinc-950 disabled:opacity-30 transition-colors rounded-full hover:bg-zinc-50"
            aria-label="Increase quantity"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="12" x2="12" y1="5" y2="19"></line>
              <line x1="5" x2="19" y1="12" y2="12"></line>
            </svg>
          </button>
        </div>
      </div>

      <!-- Buy buttons -->
      <div class="flex gap-2.5">
        <button
          type="button"
          class="product-add-to-cart flex-1 py-4 rounded-xl text-sm font-semibold tracking-[0.1em] uppercase flex items-center justify-center gap-2.5 transition-all duration-300 text-white shadow-lg shadow-zinc-200 disabled:opacity-60 disabled:cursor-not-allowed disabled:text-white" style="background-color: #000000 !important;"
          data-variant-id="{{ current_variant.id }}"
          {% unless current_variant.available %}disabled{% endunless %}
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/>
          </svg>
          <span class="btn-text">
            {%- if is_out_of_stock -%}
              {% if request.locale.iso_code == 'ar' %}نفدت الكمية{% else %}Sold Out{% endif %}
            {%- else -%}
              {% if request.locale.iso_code == 'ar' %}أضف إلى السلة{% else %}Add to Cart{% endif %}
            {%- endif -%}
          </span>
        </button>

        <button
          type="button"
          class="product-quick-buy flex-1 py-4 rounded-xl text-sm font-semibold tracking-[0.1em] uppercase flex items-center justify-center gap-2.5 transition-all duration-300 bg-white text-zinc-900 border border-zinc-300 hover:border-zinc-900 hover:bg-zinc-50 shadow-sm"
          data-variant-id="{{ current_variant.id }}"
          {% unless current_variant.available %}disabled{% endunless %}
        >
          {% if request.locale.iso_code == 'ar' %}شراء سريع{% else %}Quick Buy{% endif %}
        </button>
      </div>

      {%- if trust_badges_enabled != false -%}
      <!-- Trust Badges -->
      <div class="flex items-center justify-center gap-4 pt-1 pb-1">
        <div class="flex flex-col items-center gap-1 text-zinc-400">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          <span class="text-[9px] uppercase tracking-wider font-medium">{{ trust_badge_1 | default: "Secure Pay" }}</span>
        </div>
        <div class="w-px h-8 bg-zinc-100"></div>
        <div class="flex flex-col items-center gap-1 text-zinc-400">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13" rx="1"/><path d="M16 8h4l3 3v3h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
          <span class="text-[9px] uppercase tracking-wider font-medium">{{ trust_badge_2 | default: "Free Delivery" }}</span>
        </div>
        <div class="w-px h-8 bg-zinc-100"></div>
        <div class="flex flex-col items-center gap-1 text-zinc-400">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15"/></svg>
          <span class="text-[9px] uppercase tracking-wider font-medium">{{ trust_badge_3 | default: "Easy Returns" }}</span>
        </div>
        <div class="w-px h-8 bg-zinc-100"></div>
        <div class="flex flex-col items-center gap-1 text-zinc-400">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
          <span class="text-[9px] uppercase tracking-wider font-medium">{{ trust_badge_4 | default: "Premium" }}</span>
        </div>
      </div>
      {%- endif -%}
    </div>

  </div>
</div>

{%- unless is_modal -%}
{%- if sticky_atc_enabled != false -%}
<!-- Sticky Add to Cart Bar -->
<style>
  body.sticky-atc-active .chat-toggle,
  body.sticky-atc-active #shopify-chat,
  body.sticky-atc-active [data-spec="toggle-button"] {
    display: none !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }
</style>
<div id="StickyATC" class="fixed bottom-0 left-0 right-0 z-[9999] translate-y-full transition-transform duration-300 ease-in-out" style="background:#fff; border-top:1px solid #e5e7eb; box-shadow:0 -4px 24px rgba(0,0,0,0.08);">
  <div class="flex items-center gap-3 px-4 py-3">
    <div class="flex-1 min-w-0">
      <p class="text-xs font-semibold text-zinc-900 uppercase tracking-wide truncate sticky-atc-title">{{ product.title }}</p>
      <p class="text-xs text-zinc-500 font-mono-street sticky-atc-price">{{ product.selected_or_first_available_variant.price | money_without_trailing_zeros }}</p>
    </div>
    <button
      type="button"
      id="StickyATCBtn"
      class="shrink-0 px-6 py-3 text-white text-xs font-bold uppercase tracking-widest rounded-xl transition-all disabled:opacity-50"
      style="background-color: #000000 !important;"
    >
      {% if request.locale.iso_code == 'ar' %}أضف إلى السلة{% else %}Add to Cart{% endif %}
    </button>
  </div>
</div>
{%- endif -%}
{%- endunless -%}

  <!-- Size Chart Sub-Modal -->
  {%- if size_chart_html != blank -%}
    <div
      class="product-size-chart-modal hidden fixed inset-0 z-[99999] flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm"
    >
      <div
        class="bg-white w-full max-w-2xl rounded-2xl p-6 md:p-8 shadow-2xl flex flex-col relative max-h-[85vh] overflow-hidden"
      >
        <div class="flex items-center justify-between pb-4 border-b border-zinc-100 mb-6">
          <div class="flex items-center gap-2.5 text-zinc-900 font-semibold tracking-wide uppercase">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="12" x2="12" y1="5" y2="19"></line>
              <line x1="5" x2="19" y1="12" y2="12"></line>
              <line x1="8" x2="16" y1="16" y2="16"></line>
              <line x1="8" x2="16" y1="8" y2="8"></line>
            </svg>
            <h3 class="text-sm font-semibold text-zinc-900 tracking-wide uppercase">
              {{ size_chart_title }}
            </h3>
          </div>
          <button
            type="button"
            class="size-chart-modal-close p-1.5 rounded-full text-zinc-400 hover:text-zinc-600 hover:bg-zinc-100 transition-colors"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" x2="6" y1="6" y2="18"></line>
              <line x1="6" x2="18" y1="6" y2="18"></line>
            </svg>
          </button>
        </div>

        <div class="overflow-x-auto overflow-y-auto pr-2 flex-grow">
          <div class="size-chart-table text-sm text-zinc-600">
            {{ size_chart_html }}
          </div>
        </div>

        <div class="mt-6 pt-4 border-t border-zinc-100 text-[10px] text-zinc-400 flex items-center justify-center">
          <span>{% if request.locale.iso_code == 'ar' %}جميع القياسات بالسنتيمتر (سم) ما لم يُذكر خلاف ذلك.{% else %}All measurements are in centimeters (cm) unless specified otherwise.{% endif %}</span>
        </div>
      </div>
    </div>
  {%- endif -%}

  <!-- Product JSON for Variant Checking Script -->
  <script type="application/json" class="product-raw-json">
    {
      "id": "{{ product.id }}",
      "variants": [
        {%- for variant in product.variants -%}
          {
            "id": "{{ variant.id }}",
            "title": "{{ variant.title | escape }}",
            "options": {{ variant.options | json }},
            "available": {{ variant.available | json }},
            "inventory_quantity": {{ variant.inventory_quantity | default: 99 }},
            "price": "{{ variant.price | money_without_trailing_zeros }}",
            "price_raw": {{ variant.price }},
            "compare_at_price": "{{ variant.compare_at_price | money_without_trailing_zeros }}",
            "featured_image": {% if variant.featured_image != blank %}"{{ variant.featured_image | image_url: width: 1000 }}"{% else %}null{% endif %}
          }{%- unless forloop.last -%},{%- endunless -%}
        {%- endfor -%}
      ],
      "options": {{ product.options | json }}
    }
  </script>

  <script>
  (function() {
    function setupProductDetail() {
      const container = document.querySelector('.product-detail-wrapper');
      if (!container) return;

      const isAr = container.getAttribute('data-lang') === 'ar';
      const rawJsonEl = container.querySelector('.product-raw-json');
      let productData = null;
      try {
        if (rawJsonEl) productData = JSON.parse(rawJsonEl.textContent);
      } catch (e) {
        console.error('Failed to parse product JSON', e);
      }

      let selectedOptions = [];
      if (productData && productData.options) {
        productData.options.forEach((opt, idx) => {
          const activeColor = container.querySelector('.option-color-container[data-option-index="' + idx + '"] .color-option-btn.bg-zinc-900');
          const activeSize = container.querySelector('.option-size-container[data-option-index="' + idx + '"] .size-option-btn.bg-zinc-900');
          if (activeColor) selectedOptions[idx] = activeColor.getAttribute('data-value');
          else if (activeSize) selectedOptions[idx] = activeSize.getAttribute('data-value');
          else selectedOptions[idx] = opt.values ? opt.values[0] : '';
        });
      }

      const mainImg = container.querySelector('.product-main-image');
      const thumbBtns = container.querySelectorAll('.thumbnail-btn');
      const priceDisplay = container.querySelector('.product-price-display');
      const compareDisplay = container.querySelector('.product-compare-display');
      const atcBtn = container.querySelector('.product-add-to-cart');
      const atcText = atcBtn ? atcBtn.querySelector('.btn-text') : null;
      const quickBuyBtn = container.querySelector('.product-quick-buy');
      const stockIndicator = container.querySelector('.stock-status-indicator');
      const qtyMinus = container.querySelector('.product-qty-btn-minus');
      const qtyPlus = container.querySelector('.product-qty-btn-plus');
      const qtyVal = container.querySelector('.product-qty-display-value');

      let currentQty = 1;
      let currentVariant = productData && productData.variants ? productData.variants[0] : null;

      thumbBtns.forEach(btn => {
        btn.onclick = function(e) {
          e.preventDefault();
          const targetSrc = btn.getAttribute('data-image-url');
          if (mainImg && targetSrc) mainImg.src = targetSrc;
          thumbBtns.forEach(b => {
            b.classList.remove('border-black');
            b.classList.add('border-transparent');
          });
          btn.classList.add('border-black');
          btn.classList.remove('border-transparent');
        };
      });

      if (qtyMinus && qtyPlus && qtyVal) {
        qtyMinus.onclick = function(e) {
          e.preventDefault();
          if (currentQty > 1) {
            currentQty--;
            qtyVal.textContent = currentQty;
            if (currentQty === 1) qtyMinus.disabled = true;
          }
        };
        qtyPlus.onclick = function(e) {
          e.preventDefault();
          currentQty++;
          qtyVal.textContent = currentQty;
          qtyMinus.disabled = false;
        };
      }

      function updateVariant() {
        if (!productData || !productData.variants) return;

        currentVariant = productData.variants.find(v => {
          return v.options.every((val, idx) => selectedOptions[idx] === val);
        }) || productData.variants[0];

        if (!currentVariant) return;

        if (atcBtn) atcBtn.setAttribute('data-variant-id', currentVariant.id);
        if (quickBuyBtn) quickBuyBtn.setAttribute('data-variant-id', currentVariant.id);

        if (priceDisplay) priceDisplay.textContent = currentVariant.price;
        if (compareDisplay) {
          if (currentVariant.compare_at_price && currentVariant.compare_at_price !== '' && currentVariant.compare_at_price !== currentVariant.price) {
            compareDisplay.textContent = currentVariant.compare_at_price;
            compareDisplay.classList.remove('hidden');
          } else {
            compareDisplay.classList.add('hidden');
          }
        }

        if (mainImg && currentVariant.featured_image) {
          mainImg.src = currentVariant.featured_image;
        }

        if (atcBtn && atcText) {
          if (currentVariant.available) {
            atcBtn.disabled = false;
            if (quickBuyBtn) quickBuyBtn.disabled = false;
            atcText.textContent = isAr ? 'أضف إلى السلة' : 'Add to Cart';
            if (stockIndicator) {
              stockIndicator.className = 'stock-status-indicator text-[10px] font-semibold tracking-wide uppercase flex items-center gap-1.5 text-emerald-600';
              stockIndicator.textContent = isAr ? 'متوفر بالمخزون' : 'In Stock';
            }
          } else {
            atcBtn.disabled = true;
            if (quickBuyBtn) quickBuyBtn.disabled = true;
            atcText.textContent = isAr ? 'نفدت الكمية' : 'Sold Out';
            if (stockIndicator) {
              stockIndicator.className = 'stock-status-indicator text-[10px] font-semibold tracking-wide uppercase flex items-center gap-1.5 text-zinc-400';
              stockIndicator.textContent = isAr ? 'نفدت الكمية' : 'Sold Out';
            }
          }
        }
      }

      container.querySelectorAll('.option-color-container').forEach(colorWrap => {
        const optIndex = parseInt(colorWrap.getAttribute('data-option-index'), 10);
        const label = colorWrap.querySelector('.active-color-label');
        colorWrap.querySelectorAll('.color-option-btn').forEach(btn => {
          btn.onclick = function(e) {
            e.preventDefault();
            const val = btn.getAttribute('data-value');
            selectedOptions[optIndex] = val;
            if (label) label.textContent = val;

            colorWrap.querySelectorAll('.color-option-btn').forEach(b => {
              b.classList.remove('bg-zinc-900', 'text-white', 'border-zinc-900', 'shadow-md');
              b.classList.add('bg-white', 'text-zinc-700', 'border-zinc-200');
            });
            btn.classList.remove('bg-white', 'text-zinc-700', 'border-zinc-200');
            btn.classList.add('bg-zinc-900', 'text-white', 'border-zinc-900', 'shadow-md');

            updateVariant();
          };
        });
      });

      container.querySelectorAll('.option-size-container').forEach(sizeWrap => {
        const optIndex = parseInt(sizeWrap.getAttribute('data-option-index'), 10);
        sizeWrap.querySelectorAll('.size-option-btn').forEach(btn => {
          btn.onclick = function(e) {
            e.preventDefault();
            if (btn.disabled) return;
            const val = btn.getAttribute('data-value');
            selectedOptions[optIndex] = val;

            sizeWrap.querySelectorAll('.size-option-btn').forEach(b => {
              if (!b.disabled) {
                b.classList.remove('bg-zinc-900', 'text-white', 'border-zinc-900', 'shadow-lg', 'scale-[1.03]');
                b.classList.add('bg-white', 'text-zinc-800', 'border-zinc-200');
              }
            });
            btn.classList.remove('bg-white', 'text-zinc-800', 'border-zinc-200');
            btn.classList.add('bg-zinc-900', 'text-white', 'border-zinc-900', 'shadow-lg', 'scale-[1.03]');

            updateVariant();
          };
        });
      });

      container.querySelectorAll('.accordion-header').forEach(header => {
        header.onclick = function(e) {
          e.preventDefault();
          const item = header.closest('.accordion-item');
          const body = item ? item.querySelector('.accordion-body') : null;
          const plus = header.querySelector('.accordion-plus');
          if (!body) return;
          const isHidden = body.classList.contains('hidden');
          if (isHidden) {
            body.classList.remove('hidden');
            if (plus) plus.textContent = '−';
          } else {
            body.classList.add('hidden');
            if (plus) plus.textContent = '+';
          }
        };
      });

      const guideBtn = container.querySelector('.size-guide-trigger-btn');
      const sizeModal = container.querySelector('.product-size-chart-modal');
      const modalClose = container.querySelector('.size-chart-modal-close');
      if (guideBtn && sizeModal) {
        guideBtn.onclick = function(e) { e.preventDefault(); sizeModal.classList.remove('hidden'); };
        if (modalClose) modalClose.onclick = function() { sizeModal.classList.add('hidden'); };
        sizeModal.onclick = function(e) { if (e.target === sizeModal) sizeModal.classList.add('hidden'); };
      }

      function performAddToCart(isCheckout) {
        const targetVariantId = atcBtn ? atcBtn.getAttribute('data-variant-id') : null;
        if (!targetVariantId) return;

        const originalText = atcText ? atcText.textContent : '';
        if (atcBtn) atcBtn.disabled = true;
        if (atcText) atcText.textContent = '...';

        fetch('/cart/add.js', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
          body: JSON.stringify({ id: parseInt(targetVariantId, 10), quantity: currentQty })
        })
        .then(res => {
          if (!res.ok) throw new Error('Add to cart failed');
          return res.json();
        })
        .then(item => {
          if (isCheckout) {
            window.location.href = '/checkout';
            return;
          }
          if (atcText) atcText.textContent = '✓';
          document.dispatchEvent(new CustomEvent('cart:updated', { detail: { item: item, openDrawer: true } }));
          setTimeout(() => {
            if (atcBtn) atcBtn.disabled = false;
            if (atcText) atcText.textContent = originalText;
          }, 1200);
        })
        .catch(err => {
          console.error(err);
          if (atcBtn) atcBtn.disabled = false;
          if (atcText) atcText.textContent = isAr ? 'خطأ' : 'Error';
          setTimeout(() => { if (atcText) atcText.textContent = originalText; }, 1500);
        });
      }

      if (atcBtn) atcBtn.onclick = function(e) { e.preventDefault(); performAddToCart(false); };
      if (quickBuyBtn) quickBuyBtn.onclick = function(e) { e.preventDefault(); performAddToCart(true); };

      const stickyBar = document.getElementById('StickyATC');
      const stickyBtn = document.getElementById('StickyATCBtn');
      if (stickyBar && atcBtn) {
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              stickyBar.classList.add('translate-y-full');
              document.body.classList.remove('sticky-atc-active');
            } else {
              stickyBar.classList.remove('translate-y-full');
              document.body.classList.add('sticky-atc-active');
            }
          });
        }, { threshold: 0.1 });
        observer.observe(atcBtn);
        if (stickyBtn) stickyBtn.onclick = function() { atcBtn.click(); };
      }

      updateVariant();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', setupProductDetail);
    } else {
      setupProductDetail();
    }
  })();
  </script>
</div>