{% comment %}
  Product Modal Section
  Hidden Quickview modal shell loaded dynamically via assets/product-modal.js.
{% endcomment %}

<div 
  id="ProductQuickviewModal" 
  class="product-quickview-modal-container hidden fixed inset-0 z-[9999] bg-[#f6f5f2]/80 backdrop-blur-md flex items-end sm:items-center justify-center sm:p-4 md:p-8 opacity-0 transition-opacity duration-300"
>
  <div 
    class="modal-content-wrapper relative bg-white w-full max-w-5xl h-[92dvh] sm:h-auto sm:max-h-[90vh] lg:h-[85vh] flex flex-col lg:flex-row overflow-y-auto lg:overflow-hidden rounded-t-3xl sm:rounded-3xl shadow-2xl transform scale-95 transition-transform duration-300"
  >
    <!-- Filled dynamically by product-modal.js -->
    <div class="h-64 w-full flex items-center justify-center text-zinc-400 font-mono-street text-xs uppercase tracking-widest">
      Loading details...
    </div>
  </div>
</div>

<script src="{{ 'product-modal.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Product Quickview Modal",
  "settings": []
}
{% endschema %}
